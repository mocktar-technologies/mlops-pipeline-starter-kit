# Local development and CI tooling. Never installed into a runtime image.

-c serving.txt

pytest==9.1.1
pytest-cov==7.1.0
# starlette's TestClient now asks for httpx2; httpx 0.x raises a
# StarletteDeprecationWarning on import, and this suite fails on new
# deprecations from its own packages.
httpx2==2.12.0
ruff==0.16.7
black==26.5.1
mypy==2.3.1
types-PyYAML==6.0.12.20260906
pandas-stubs==3.0.5.260730

# Needed by the three static checks in scripts/, which the CI lint job runs. These are
# not optional extras: without them tf_lint.py, helm_lint.py and check_metric_names.py
# exit immediately saying the import is missing, and the job that is supposed to catch a
# missing module input or a metric nothing emits fails before it checks anything.
python-hcl2==8.1.4
PyYAML==6.0.3

# Used only by the test suite, to build a tiny ONNX fixture graph without
# pulling PyTorch into the serving test run.
onnx==1.20.0
